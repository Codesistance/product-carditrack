<?xml version="1.0" encoding="UTF-8" ?>
<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <!-- ── Standard MAUI utility colours ── -->
    <Color x:Key="White">White</Color>
    <Color x:Key="Black">Black</Color>
    <Color x:Key="OffBlack">#1f1f1f</Color>

    <Color x:Key="Gray100">#E1E1E1</Color>
    <Color x:Key="Gray200">#C8C8C8</Color>
    <Color x:Key="Gray300">#ACACAC</Color>
    <Color x:Key="Gray400">#919191</Color>
    <Color x:Key="Gray500">#6E6E6E</Color>
    <Color x:Key="Gray600">#404040</Color>
    <Color x:Key="Gray900">#212121</Color>
    <Color x:Key="Gray950">#141414</Color>

    <!-- ── Figma design tokens ── -->
    <Color x:Key="GradientDarkBlue">#174E86</Color>
    <Color x:Key="GradientBlue">#1884DC</Color>
    <Color x:Key="GradientTeal">#36C09B</Color>
    <Color x:Key="Primary">#1884DC</Color>
    <Color x:Key="PrimaryDark">#174E86</Color>
    <Color x:Key="ActiveIndicator">#1A73E8</Color>
    <Color x:Key="InactiveIndicator">#D1D5DC</Color>
    <Color x:Key="HeadingText">#343434</Color>
    <Color x:Key="BodyText">#727272</Color>
    <Color x:Key="MutedText">#939DAA</Color>
    <Color x:Key="InputBackground">#F2F2F4</Color>
    <Color x:Key="InputBorder">#DCDFE4</Color>
    <Color x:Key="InputFocusBorder">#1884DC</Color>
    <Color x:Key="SelectedOptionBackground">#F0F6FF</Color>
    <Color x:Key="Divider">#E2E8F0</Color>
    <Color x:Key="ErrorRed">#E53E3E</Color>
    <Color x:Key="StrengthWeak">#E53E3E</Color>
    <Color x:Key="StrengthMedium">#1884DC</Color>
    <Color x:Key="StrengthStrong">#36C09B</Color>

    <!-- ── Dashboard status tokens (M1-09) ── -->
    <Color x:Key="StatusGreen">#36C09B</Color>
    <Color x:Key="StatusYellow">#F0A92E</Color>
    <Color x:Key="StatusOrange">#ED7B2F</Color>
    <Color x:Key="StatusRed">#E53E3E</Color>
    <Color x:Key="StatusUnknown">#939DAA</Color>
    <!-- Data-freshness "blue" tier only — deliberately not part of the green/yellow/orange/red
         clinical-severity set above, so the two concerns stay visually distinct. -->
    <Color x:Key="StatusBlue">#1884DC</Color>
    <Color x:Key="SkeletonGray">#E7EBF0</Color>
    <Color x:Key="SleepPurple">#7C6FDC</Color>
    <Color x:Key="StaleBannerBackground">#FFF7E8</Color>

    <!-- ── Surface tokens (Figma 101:2805) ──
         Every screen floats its cards on this tinted ground rather than on white, which is why
         the card treatment is elevation + radius rather than a hairline stroke. -->
    <Color x:Key="PageGround">#E8F1F4</Color>
    <Color x:Key="CardShadow">#014297</Color>
    <Color x:Key="ProgressTrack">#E5E7EB</Color>
    <!-- Hairline for cards that sit ON another card rather than on the tinted ground: nested
         elevation reads as clutter, so those tiles take an outline instead (see OutlinedCard).
         Darker than Divider — it has to hold its own against white, not against PageGround. -->
    <Color x:Key="CardOutline">#D7DFE9</Color>

    <!-- ── Key Metric Trends chart annotations (M1-13) ──
         The two things a reading is drawn against: this member's own baseline, and the published
         typical-adult range behind it. Neutral on purpose — they are context, not a severity, so
         they stay out of the status set above; and out of StatusUnknown in particular, which is
         the accent the line of an unrated metric (SpO2, breathing rate) already wears. -->
    <Color x:Key="ChartReferenceBand">#5C6672</Color>
    <Color x:Key="ChartBaselineRule">#343434</Color>

    <!-- ── Per-metric ink ──
         Each one is the stroke colour of that metric's own glyph, lifted out of the SVG so the
         trend line and the icon above it are the same colour rather than two things that happen to
         sit on the same card. Identity, not severity: how the member is doing is the pill's job,
         and a line that changed colour with status would leave the reader relearning which metric
         they were looking at every time it moved. Skin temperature and blood oxygen share an ink
         because their glyphs do — they never appear on the same card, so nothing is ambiguous. -->
    <Color x:Key="MetricStepsInk">#1884DC</Color>
    <Color x:Key="MetricHeartInk">#E53E3E</Color>
    <Color x:Key="MetricSleepInk">#7C6FDC</Color>
    <Color x:Key="MetricTemperatureInk">#1F8A72</Color>
    <Color x:Key="MetricSpO2Ink">#1F8A72</Color>
    <Color x:Key="MetricBreathingInk">#3E8AC7</Color>

    <!-- Metric row icon tile — Figma rgba(201,225,255,0.41) -->
    <Color x:Key="MetricTileTint">#69C9E1FF</Color>
    <!-- Header refresh button — Figma rgba(255,255,255,0.29) -->
    <Color x:Key="HeaderButtonTint">#4AFFFFFF</Color>

    <!-- Status pills: tint is the status colour at 15%, ink is the colour itself. -->
    <Color x:Key="PillGreenBackground">#2636C09B</Color>
    <Color x:Key="PillYellowBackground">#26F0A92E</Color>
    <Color x:Key="PillOrangeBackground">#26ED7B2F</Color>
    <Color x:Key="PillRedBackground">#26E53E3E</Color>
    <Color x:Key="PillNeutralBackground">#26939DAA</Color>

    <!-- Alert icon tiles — Figma rgba(239,68,68,0.14), generalised across the severity set -->
    <!-- Quick actions (dashboard), in the app's own blue rather than a severity colour — these are
         ordinary controls, and only the one that dials an emergency contact wears red. The row
         used to be white cards on a white card, which needed a drop shadow to separate them and
         read as three pieces of chrome floating on a fourth.
         Filled at 24% rather than the 14% the alert tiles use: a wash that light reads as a
         highlighted region, and these have to read as things to press. The glow on
         QuickActionTile does the rest of that work. -->
    <!-- Opaque, not a translucent wash. Android casts a view's shadow from its outline and all but
         drops it for a see-through fill, so the glow these tiles are supposed to sit in simply did
         not render while they were alpha tints. These are the same two colours flattened onto the
         white card they sit on — identical to the eye, and they cast. -->
    <Color x:Key="QuickActionTint">#C8E2F7</Color>

    <!-- The SOS tile at the same weight, so one tile is not visibly fainter than the three beside
         it. Its own colour rather than AlertTileRed, which the alert cards elsewhere are drawn at
         and should not be dragged along by a dashboard decision. -->
    <Color x:Key="QuickActionTintRed">#F9D1D1</Color>

    <Color x:Key="AlertTileRed">#24E53E3E</Color>
    <Color x:Key="AlertTileOrange">#24ED7B2F</Color>
    <Color x:Key="AlertTileYellow">#24F0A92E</Color>
    <Color x:Key="AlertTileNeutral">#24939DAA</Color>

    <!-- Alert card quick actions (M1-10) — Figma rgba(24,132,220,0.18). Distinct from the
         severity tints above on purpose: these are the caregiver's controls, not a reading. -->
    <Color x:Key="AlertActionBackground">#2E1884DC</Color>

    <!-- ── Device dataset pills (M1-15) ──
         One tint/ink pair per DatasetFamily, so a device card's pills group by the kind of
         reading they carry. The heart pair is deliberately rosier than ErrorRed: a pill sits
         inches from the DISCONNECTED status chip and must not read as an alarm. -->
    <Color x:Key="DatasetActivityBackground">#E8F3FD</Color>
    <Color x:Key="DatasetActivityText">#10659F</Color>
    <Color x:Key="DatasetHeartBackground">#FDEEF1</Color>
    <Color x:Key="DatasetHeartText">#B93A55</Color>
    <Color x:Key="DatasetSleepBackground">#EEECFB</Color>
    <Color x:Key="DatasetSleepText">#5A4EBF</Color>
    <Color x:Key="DatasetBodyBackground">#E4F6F2</Color>
    <Color x:Key="DatasetBodyText">#1F8A72</Color>
    <Color x:Key="DatasetOtherBackground">#EEF1F5</Color>
    <Color x:Key="DatasetOtherText">#5C6672</Color>
    <!-- A connection sharing nothing is a problem to fix, not a neutral fact, so its pill takes
         the caution tint the NEEDS RECONNECT status chip uses rather than the grey "other" pair. -->
    <Color x:Key="DatasetWarningBackground">#FFF3DE</Color>
    <Color x:Key="DatasetWarningText">#A9741A</Color>

    <!-- ── Solid colour brushes ── -->
    <SolidColorBrush x:Key="CardShadowBrush" Color="{StaticResource CardShadow}"/>
    <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource Primary}"/>
    <SolidColorBrush x:Key="WhiteBrush" Color="{StaticResource White}"/>
    <SolidColorBrush x:Key="BlackBrush" Color="{StaticResource Black}"/>

    <SolidColorBrush x:Key="Gray100Brush" Color="{StaticResource Gray100}"/>
    <SolidColorBrush x:Key="Gray200Brush" Color="{StaticResource Gray200}"/>
    <SolidColorBrush x:Key="Gray300Brush" Color="{StaticResource Gray300}"/>
    <SolidColorBrush x:Key="Gray400Brush" Color="{StaticResource Gray400}"/>
    <SolidColorBrush x:Key="Gray500Brush" Color="{StaticResource Gray500}"/>
    <SolidColorBrush x:Key="Gray600Brush" Color="{StaticResource Gray600}"/>
    <SolidColorBrush x:Key="Gray900Brush" Color="{StaticResource Gray900}"/>
    <SolidColorBrush x:Key="Gray950Brush" Color="{StaticResource Gray950}"/>

    <!-- ── Gradient brushes ── -->
    <LinearGradientBrush x:Key="GradientButtonBrush" StartPoint="0,0.5" EndPoint="1,0.5">
        <GradientStop Color="{StaticResource GradientDarkBlue}" Offset="0.0" />
        <GradientStop Color="{StaticResource GradientBlue}" Offset="0.458" />
        <GradientStop Color="{StaticResource GradientTeal}" Offset="1.0" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="GradientButtonLightBrush" StartPoint="0,0.5" EndPoint="1,0.5">
        <GradientStop Color="#4D174E86" Offset="0.0" />
        <GradientStop Color="#4D1884DC" Offset="0.458" />
        <GradientStop Color="#4D36C09B" Offset="1.0" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="WelcomePageBackground" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#F0F6FF" Offset="0.0" />
        <GradientStop Color="#FFFFFF" Offset="0.55" />
        <GradientStop Color="#FFFFFF" Offset="1.0" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="HeroBottomFade" StartPoint="0,1" EndPoint="0,0">
        <GradientStop Color="#E6FFFFFF" Offset="0.0" />
        <GradientStop Color="#00FFFFFF" Offset="0.72" />
    </LinearGradientBrush>

    <!-- ── Status hero card gradients (M1-09) ── -->
    <LinearGradientBrush x:Key="HeroGreenBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#2AA184" Offset="0.0" />
        <GradientStop Color="#36C09B" Offset="1.0" />
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="HeroYellowBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#D89422" Offset="0.0" />
        <GradientStop Color="#F0A92E" Offset="1.0" />
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="HeroOrangeBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#D2691F" Offset="0.0" />
        <GradientStop Color="#ED7B2F" Offset="1.0" />
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="HeroRedBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#C42F2F" Offset="0.0" />
        <GradientStop Color="#E53E3E" Offset="1.0" />
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="HeroUnknownBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#174E86" Offset="0.0" />
        <GradientStop Color="#1884DC" Offset="1.0" />
    </LinearGradientBrush>
    <!-- Paused monitoring: deliberately outside the status palette so it can't be mistaken
         for a health reading. -->
    <LinearGradientBrush x:Key="HeroPausedBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#5C6672" Offset="0.0" />
        <GradientStop Color="#7E8794" Offset="1.0" />
    </LinearGradientBrush>

    <!-- ── Header band (Figma 101:2872) ──
         Figma draws this as the brand gradient (#174E86 → #1884DC → #36C09B) on an oversized
         rect, blurred 100px and masked to the band, so only a slice of the ramp is visible and
         its right edge feathers out to the page ground. MAUI has no cheap blur, so the visible
         slice is baked into the stops below and the feather is the wash layer underneath.
         Stops are sampled straight off the rendered Figma node, top and bottom of the band.
         Both brushes are consumed by Controls/HeaderBand, which every header in the app uses. -->
    <LinearGradientBrush x:Key="HeaderBandBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#1E8FC7" Offset="0.0" />
        <GradientStop Color="#3DADBE" Offset="1.0" />
    </LinearGradientBrush>

    <!-- The feather. Alpha at each stop is solved from the rendered Figma node: sample the
         band at that fraction of its width, then work out how much white sits over the base
         ramp. The profile came out the same at the top and bottom of the band, which is why one
         horizontal brush models it. Note the slight lift at the far left — the blur's own
         falloff — and the dip to nothing around 30%, where the band is at its most saturated. -->
    <LinearGradientBrush x:Key="HeaderBandWashBrush" StartPoint="0,0.5" EndPoint="1,0.5">
        <GradientStop Color="#1AFFFFFF" Offset="0.0" />
        <GradientStop Color="#05FFFFFF" Offset="0.18" />
        <GradientStop Color="#00FFFFFF" Offset="0.30" />
        <GradientStop Color="#04FFFFFF" Offset="0.41" />
        <GradientStop Color="#16FFFFFF" Offset="0.52" />
        <GradientStop Color="#39FFFFFF" Offset="0.64" />
        <GradientStop Color="#6BFFFFFF" Offset="0.75" />
        <GradientStop Color="#A7FFFFFF" Offset="0.86" />
        <GradientStop Color="#C4FFFFFF" Offset="0.93" />
        <GradientStop Color="#D1FFFFFF" Offset="1.0" />
    </LinearGradientBrush>

</ResourceDictionary>