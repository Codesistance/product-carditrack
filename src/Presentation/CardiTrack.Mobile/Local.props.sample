<!-- Copy to Local.props (git-ignored) to override build-time config for local development.
     Values here win over the defaults in CardiTrack.Mobile.csproj. -->
<Project>
	<PropertyGroup>
		<!-- Local API (dotnet run in CardiTrack.API listens on http://localhost:5230):
		       Android emulator: http://10.0.2.2:5230  (cleartext allowed via network_security_config.xml)
		       Windows/iOS sim:  http://localhost:5230
		     Default (unset): https://api.dev.carditrack.com -->
		<!-- <ApiBaseUrl>http://10.0.2.2:5230</ApiBaseUrl> -->

		<!-- Auth0 dev tenant (public identifiers, not secrets) -->
		<!-- <Auth0Domain>your-tenant.eu.auth0.com</Auth0Domain> -->
		<!-- <Auth0ClientId>yourNativeAppClientId</Auth0ClientId> -->
		<!-- <Auth0Audience>https://api.carditrack.com</Auth0Audience> -->

		<!-- Mobile APM (optional — unset means monitoring is off, which is the norm for
		     local builds). Logs and traces always; RUM (sessions, views, errors, crash
		     reporting) when ApplicationId is set. Raw JSON is accepted here; CI stamps the
		     same value base64-encoded. Embed-safe identifiers only, never runtime secrets.
		     Our org is on UK1, which the SDK cannot name: give the bare intake host as
		     IntakeHost and every feature is routed there (Site is then informational). See
		     docs/technical/apm_setup_runbook.md §5. Android/iOS only. -->
		<!-- <ApmEngine>Datadog</ApmEngine> -->
		<!-- <ApmData>{"ClientToken":"pub...","ApplicationId":"...","Site":"Uk1","IntakeHost":"browser-intake-uk1-datadoghq.com"}</ApmData> -->
	</PropertyGroup>
</Project>
