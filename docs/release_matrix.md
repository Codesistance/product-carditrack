# CardiTrack Release Matrix

**The canonical release plan.** When any other document (manifest roadmap, UI screen specs, API priorities) disagrees with this matrix, this matrix wins. Platform docs keep their own MVP numbering (mobile has 3 MVPs, web has 4); this matrix maps them onto shared release waves.

## Release Waves

| Wave | Roadmap window | Mobile release | Web release | Theme |
|------|---------------|----------------|-------------|-------|
| **R1 — Core Monitoring** | Q4 2026 (MVP completion + beta) | Mobile MVP 1 | Web MVP 1 | Sign up, add CardiMembers, connect Fitbit, dashboard, all alert types, acknowledgment |
| **R2 — Management & Billing** | Q1 2027 (public launch) | Mobile MVP 2 (part) | Web MVP 2 | Trend charts, notification preferences, **subscriptions/billing**, Garmin, expanded exports; **AI pipeline rollout** on GCP — Pub/Sub + Cloud Run ([llm_design.md](./llm_design.md)) |
| **R3 — Family & Multi-Member** | Q2 2027 | Mobile MVP 2 (part) | Web MVP 3 | Family invitations, shared notes, multi-member management, test-result scanning |
| **R4 — Native & Offline** | Q3 2027 | Mobile MVP 3 | Web MVP 4 | Biometric login, offline support, push actions, widgets/PWA, SNOMED CT export |

API endpoint priorities (P0–P2 in [/execution/backend/api/](./execution/backend/api/readme.md)) are **relative to the wave in which the feature ships**, per the matrix below.

## Feature Matrix

Legend: wave number = ships in that wave; — = not planned for that surface. **Status** is as of **September 18, 2026**: ✅ Shipped · 🔶 In progress / partial · ⬜ Not started.

| Feature | API | Mobile | Web | Plan gate | Status (Sep 18, 2026) |
|---------|-----|--------|-----|-----------|----------------------|
| Auth0 Universal Login (email + password) | R1 | R1 | R1 | — | ✅ Shipped, incl. email-verification gate |
| Social sign-in (Google / Apple via Auth0) | R1 | R1 | R1 | — | 🔶 **Mobile wired** (Auth0 PKCE via system browser; cancel restores button opacity). Web pending. Production tenant social connections remain an ops gate |
| Onboarding (atomic account + org + CardiMember setup) | R1 | R1 | R1 | — | ✅ Shipped, incl. org-orphaning cleanup fix (PR #5) and sex capture at M1-04 |
| CardiMember CRUD + profile | R1 | R1 | R1 | Member limit by tier | 🔶 API + mobile shipped (GET/PUT/DELETE `/api/v1/cardimembers/{id}` + detail/edit screens, member Phone on M1-14); web not started (template-stage). Tier member limit not yet enforced. Includes the **member photo**, which is Tier 1 data: blob deletes on replace, removal and soft delete are best-effort, with `OrphanedPhotoCleanupWorker` as the enforcement backstop so a blob cannot outlive the membership ([data_protection_architecture.md](./technical/data_protection_architecture.md) §5) |
| Emergency contacts, medical notes (encrypted) | R1 | R1 | R1 | — | 🔶 API + mobile shipped inside member CRUD (medical notes AES-encrypted at rest); web not started (template-stage) |
| Consent recording (per-metric) | R1 | R1 | R1 | — | ⬜ **Not started.** Two narrower consents do exist and are not this one: `ExportConsent` (recorded per export, and at the caregiver's choice remembered for just this export, 1 week, 2 weeks or 1 month — `ExportConsentPolicy.RememberChoices`; the month is 30 days, not a calendar month — PRs #557, #564) and `CardiMember.EnvironmentalContextConsentGranted`. Neither records what a family agreed to *collect*, per metric, at connection time |
| Fitbit connection (Google Health API — server OAuth + REST client) | R1 | R1 | R1 | **100 connected wearers max until Google verification passes** | ✅ Client shipped (PR #10, migrated off legacy Fitbit Web API); registration done 2026-08-07; 🔶 live-wearer field verification pending |
| Google Health webhook subscriptions (push ingestion) | R2 | R2 | R2 | — | 🔶 **Shipped in dev ahead of R2** — receiver live (`POST /webhooks/google-health`) + Subscriber registered 2026-08-10, feeding Pub/Sub + the aggregator; prod off until the pipeline rollout. 10-minute Worker polling (✅ shipped) remains the guaranteed fallback |
| Device management (status, primary, reconnect, remove) | R1 | R1 | R1 | — | 🔶 API + mobile shipped (remove / set-primary / sync / refresh endpoints + device management screen, M1-15); web not started (template-stage). **Caregiver history re-pull shipped 2026-09-09** (#549) — device-card action, 202 endpoint, chunked Worker executor, 48-hour cooldown |
| **Wearer-completed device connection (invite link / QR)** | R1 | R1 | — | — | 🔶 **Shipped in dev 2026-09-18** (#1131, #1134–#1137). The caregiver no longer has to hold the wearer's Google credentials: `DeviceInvitesController` mints a short-lived invite (create / poll / revoke under `/api/v1/cardimembers/{id}/device-invites`), the mobile `InviteWaitPage` shows it as a shareable link and a QR code and watches for completion (`DeviceInviteWatch`), and the wearer opens an anonymous hosted page — `WearerConnectController` at `/connect` with `GET ""`, `POST start`, `POST decline` — that carries CardiTrack's logo, Google's required shape, and a CSP narrowed to the provider it redirects to. **This does not make the wearer an app user** (decision log #10): they consent once at their own provider and get no account, no readings and no notifications |
| Dashboard + daily health summary | R1 | R1 | R1 | — | 🔶 Per-member dashboard endpoint + mobile dashboard shipped (unresolved-alerts strip, weather popup, delayed "Loading", single-sentence AI status, 30-day baseline gate); web dashboard not started (web app is still template-stage) |
| Statistical alerts (all 5 launch types) + acknowledgment/notes | R1 | R1 | R1 | — | 🔶 **Shipped** via `StatisticalAlertWorker` (PR #118; since 2026-09-19 `StatisticalAlertService` in the pipeline's assess job, findings judged by MedGemma) and `InactivityDetectionWorker` (PR #116); `AlertsController` (6 routes incl. GET detail + undo-ack) + `AlertsPage` (M1-10) + `AlertDetailPage` (M1-11/12/16, rule-specific chart) serve them. Red/Orange alerts push via the delivery spine; Safety-class nudges (`DEVICE_AUTH_BROKEN`, three-tier `DEVICE_BATTERY_LOW`) also push ([notification_engine.md](./technical/notification_engine.md)). Notes/photos not started. SSA eigen-decomposition is in-process Math.NET ([mathnet_numerics.md](./technical/mathnet_numerics.md)). *Caveat: `InactivityDetectionWorker` was throwing on every tick until fixed 2026-08-13 — inactivity alerting was silently dead before that date* |
| AI insights + chat endpoints (MedGemma via Ollama on Cloud Run; Gemini 2.0 Flash) | R1 | R1 | R1 | — | ✅ Shipped (synchronous endpoints; the R2 event-driven pipeline is separate). Chat routes to a closed catalogue of rungs — status, analysis, inference, investigation and the rest — declared in `ChatWorkflowCatalogue`, each with the datasets it may read and the class of claim it may make |
| **Chat that changes things (alert settings, journal books)** | R2 | R2 | — | — | 🔶 **Shipped in dev 2026-09-17** (#1116, #1117, #1120, #1124). The first chat rungs with a side effect: `AlertSettingsChatActions` reads and proposes one alert change at a time, `JournalChatActions` shows, lists, discards and rewrites CardiJournal books. **Anything destructive takes two turns** — the action is offered, held on the session for ten minutes, and carried out only on a plain yes matched in code (no model call on the confirming turn, so it cannot be talked into a different action than the one offered), with one statement taking the offer off the row so a doubled yes acts once. The book is composed before that statement and stored after it, so no model call runs inside a transaction. Access is re-checked at apply time, the member's name is redacted before the journal resolver, and a journal change is audited as a change (#1120). *Remaining: nothing else is reachable from chat — no member edit, no device action, no export* |
| **"Something to try" (Advise card)** | R2 | R2 | — | — | 🔶 **Shipped in dev 2026-09-18** (#1103, #1107, #1108, #1130). A per-member wellness suggestion with its summary and the reference it drew on (`AdviseResponse`, `IMemberAdviseRepository`), rendered on Member Detail and the dashboard hero. Written by the clinical + rewrite pair on the half-hourly digest pass, but gated by `AdviseCadence`: **at most five successful writes per member local day**, spaced across the waking window (the complement of the anchor caregiver's quiet hours), counting the longer of UTC and wall-clock so spring-forward cannot add a sixth. The cadence survives navigation, not just the page (#1108), and a `PromptVersion` bump is due on the next waking pass rather than at 03:00. **The only rung allowed to claim `ChatClaimClass.Suggestion`** — everything else states values or positions them against a reference, never whether that is good. *Remaining: telling a caregiver a suggestion has been rewritten is in flight on `cursor/advise-notify-c95c`, not merged* |
| Reports (health report generation) | R1 | R1 | R2 | Complete Care | ✅ **Shipped** — PDF, CSV and FHIR R4 all render (see the export row); reports are durable (Postgres row + GCS object) rather than cache-backed, with a 7-day window swept by `ExpiredReportCleanupWorker` |
| **Data-completeness notifications (in-app)** | R1 | R1 | R3 | — | ✅ **Shipped** — detection worker, 8 rules, inbox + dashboard card + safety banners + mute management. In-app only by decision; the staleness rule defers to `InactivityDetectionWorker`'s faster device-silence alert ([notification_engine.md](./technical/notification_engine.md)) |
| Push notification registration | R1 | R1 | — | — | ✅ **Shipped** — brought forward from its original R2 placement ([notification_engine.md](./technical/notification_engine.md) Phase 3). FCM HTTP v1 relay (APNs passthrough), device tokens, delivery outbox, 120s/300s/900s escalation ladder, quiet hours. iOS notification service extension deferred (needs Mac-based CI verification this environment doesn't have). **Delivery is watched by a canary**: `PushCanaryWorker` sends a real Safety-category push to a configured fleet of test devices every 15 minutes and checks whether the *previous* run's canary was acked ([notification_engine.md](./technical/notification_engine.md) §13) — provider outages, expired credentials and a broken APNs certificate are otherwise silent until someone reports them during an actual emergency. The fleet is configured rather than auto-discovered, so it can never page a real caregiver |
| **Quiet reassurance (the weekly all-clear)** | R1 | R1 | — | — | 🔶 **Shipped in dev, no row until now.** `QuietReassuranceWorker` finds the members nothing has been raised about for a week or more and pushes them an all-clear — the one notification that is good news. Pure rule evaluation against data already in the database, no AI call, so it lives in the Worker beside `StatisticalAlertWorker` whose candidate filter and per-member isolation it mirrors. **Swept daily, felt weekly**: the dedup key is derived from the stretch's week number, so a member crossing the seven-day line is caught the next day rather than waiting for a weekly tick, but a caregiver still hears it once a week per member |
| Health data export — PDF, CSV, FHIR R4 | R1 | R1 | R2 | Complete Care (R2) | 🔶 **API + mobile shipped** — all three formats render and export is open to every account until plan gating exists (the 2026-09-06 `IEntitlementService` gate was removed 2026-09-07: nothing else is gated today, and it locked every lapsed-trial account out), with max-5-member and 365-day caps enforced. Mobile M1-17 (`ExportHealthDataPage`) ships save-and-share only; **server-side email delivery is not built** — the solution has no email sender of any kind ([screens.md](./execution/ui/mobile/mvp1/screens.md)). Web is R2. **Since 2026-09-10 every export is gated on recorded consent** (#557): an `ExportConsent` row per export, proved either by password (checked against Auth0) or by a device-local biometric on an already signed-in session (`ExportConsentMethod`), and reusable for 1 week, 2 weeks or 1 month at the caregiver's choice (#564), and the export no longer cancels when that popup opens (#565). The PDF is now a document a caregiver can hand to a doctor (#562) and carries trend charts, with journals, notices and graphs-only among the things exportable (#559, #1071) |
| Baseline learning progress | R1 | R1 | R1 | — | 🔶 Daily `BaselineCalculationWorker` + mobile learning screen shipped; web not started (web app is still template-stage) |
| Monitoring pause / resume | R1 | R1 | R1 | — | 🔶 API + mobile shipped (pause/resume endpoints; paused members excluded from sync scheduling); web not started (template-stage) |
| **Account deletion / erasure endpoint** | R1 | R1 | R2 | — | 🟩 **Live in dev (2026-09-14).** A caregiver can ask, in the app: Settings → Delete my account takes a tick box, a device biometric and a final confirmation, then calls `POST /api/v1/users/me/deletion` and signs them out. The request refuses the account from that moment and stops collection for any member it leaves without a caregiver; signing in within 30 days cancels it. `RetentionWorker` (M6, daily 05:00 UTC) then erases: `IAccountErasureService` erases the members this caregiver was the last active watcher of via `IMemberErasureService`, releases the ones somebody else still watches, and removes the account's own rows. The same worker enforces the 90-day member-chat period (#488). Both cascades are tested against a real Postgres. **In effect in dev only.** CI run 34901698217 applied `AddUserDeletionRequest` and `AddCardiMemberCreationKeys` and deployed all six services on 2026-09-14; the worker's first dev sweep runs in **rehearsal** (`retention_worker_dry_run = true`) so the log can be read before anything is erased. **Prod has none of this** — it is a separate dispatch. [manual_erasure_runbook.md](./technical/manual_erasure_runbook.md) remains the path for prod and for the one step the worker still does not do (the confirmation email — the solution has no email sender at all, so this is a new dependency rather than a missing call). **Upstream OAuth revocation shipped 2026-09-14** (#148 item 4): disconnecting a device, removing a member and erasing an account each end the grant at the provider before the token is discarded, and an unconfirmed revocation is named in the erasure report rather than swallowed. Was: 🟨 **Partly built** — the ask shipped without the thing that erases. Was: ⬜ **Not started — but the promise is already published.** privacy-policy §5 and the deletion page commit to erasure within **30 days of a verified request**, including inside the Google Health API section. Nothing delivers it: member and device deletes are **soft** (`IsActive` flip + token discard), no PHI row is removed, and with almost no foreign keys a deleted member **orphans** its `ActivityLogs`/`Alerts`/`PatternBaselines`/`DeviceConnections` rows, which stay live and queryable ([data_protection_architecture.md](./technical/data_protection_architecture.md) findings 5–6). Interim control: [manual_erasure_runbook.md](./technical/manual_erasure_runbook.md), viable only at the current sub-100-wearer cap. `ErasureWorker`, `erasure_requests`/`erasure_ledger` and `SubjectDataMap` are designed (P2/P3) and unbuilt |
| **30-day trial (no billing UI)** | R1 | R1 | R1 | — | ✅ Shipped — trial provisions the **Complete Care tier** for 30 days |
| Region-localized phone input (UK groundwork) | R1 | R1 | R1 | — | ✅ Shipped (PR #8) |
| **Terms and privacy policy inside the app** | — | R1 | n/a | — | ✅ **Shipped, no row until now.** `LegalDocumentPage` shows both documents in a modal, linked from the account-creation form beside the checkbox that agrees to them. Fetched from the site with `?embed`, which returns the same document without its own chrome — so the published policy and the one a caregiver agrees to are the same text, not a copy that can drift. A modal rather than the phone's browser: an app switch would leave a half-filled form behind it. Depends on the branding site staying the source, which makes it a cross-repo contract worth naming here |
| Health-data disclosure (Google-mandated in-app disclosure) | R1 | R1 | R1 | — | ✅ Web (PR #9) and mobile (Dashboard banner, 2026-09-13) — both record the dismissal on the account via `GET /api/v1/users/me/health-data-disclosure` and `POST /api/v1/users/me/health-data-disclosure/dismiss` |
| Observability (Datadog APM, opt-in metrics via `Apm:Engine`) | R1 | R1 | — | — | ✅ Shipped (PR #4). **Mobile telemetry now reaches Datadog too** — the MAUI Datadog SDK has no entry for this org's UK1 site, so the app relays its own Error-and-above lines (including the last frames before an unhandled crash) through `MobileDiagnosticsController`, re-emitted under the mobile service name (#1102). Anonymous by necessity with a compiled-in limiter key plus a per-IP rate limit. **The Settings diagnostics toggle does not gate this relay** — `DiagnosticsConsent` governs the Datadog SDK's tracking consent (`MobileApm.TrackingConsent`) and the app's own opt-in sends (#1077); the Error-and-above relay ships regardless, which is why it is disclosed in the privacy policy instead (branding repo #73). A caregiver can also send the log off their phone by hand (#1095). **Open question: whether the relay should honour the toggle or stay ungated as a safety channel** |
| **Google restricted-scope verification + annual CASA** | R1→R2 gate | R1→R2 gate | R1→R2 gate | **Blocks >100 connected wearers** | ⬜ Not started — cross-wave external gate: Gate 1 Trust & Safety review + Gate 2 annual CASA ($500–$4,500, 2–6 weeks; combined runway 4–8 weeks). **Now four scopes to justify, not three** — `settings.readonly` added 2026-08-13 (PR #262) for wearable battery; added deliberately *before* submission, since a scope added afterwards needs its own second review. The wearer-facing consent page built on 2026-09-18 was given Google's required shape and branding (#1135, #1137) so that the flow a reviewer walks is the one that ships. See [user_onboarding_process.md Step 6](./technical/user_onboarding_process.md), [oauth_clients.md](./technical/oauth_clients.md) and [runbook step 1b](./technical/production_setup_runbook.md) |
| Wearable battery level + `DEVICE_BATTERY_LOW` safety notification | R1 | R1 | — | Needs `settings.readonly` granted | 🔶 Code, migration and Terraform shipped (PR #262); **three tiers** (Warning ≤30% / Urgent ≤20% / Critical ≤10% or Empty band), freshness **12 hours**, Safety-class push. **Blocked on the console scope change** (runbook step 1b) in both devices projects. Existing connections report no battery until the wearer reconnects — degrades silently by design, never fails a sync |
| **Legacy Fitbit Web API sunset — 2026-09-30** | external deadline | external deadline | external deadline | — | ✅ **Not exposed — confirmed 2026-09-05.** CardiTrack has never called `api.fitbit.com`: the repo's only reference is the informational `ApiEndpoint` on the 2026-03 seed row, rewritten to `health.googleapis.com` by the 2026-08-13 Pixel Watch migration and never read at runtime; every appsettings and tfvars points at Google Health API + Google OAuth, and every stored token is Google-issued. Nothing to migrate, no wearer reconnects. The live-wearer field-population check stays open on its own merits ([oauth_clients.md](./technical/oauth_clients.md) step 5b) |
| **CardiJournal — the Weekbook** | R2 | R2 | — | Complete Care | 🔶 **Shipped in dev, API and mobile.** `DigestAudience.Weekbook` + `IX_DigestEntries_OneWeekbookPerWeek` (its own partial index — a Weekbook and a Daybook share a `LocalDate` most weeks, so one index across both audiences would refuse the second write), `GenerateDueWeekbooksAsync` riding the existing half-hourly `--job digest` pass, `WeekbookPrompt`, and `?audience=weekbook` on the insights digest endpoints. Due on the member's own `JournalWeekStartsOn` once their `WeekbookLocalTime` passes, covering the seven days ending the evening before and dated by the week's **last day**, written once. **Guard: at least 4 of 7 days must carry readings** — an unmeasured week gets no account, because one would have to speak for the days that are missing. **A raw reassessment of the week from that week's measurements, not a summary of its Daybooks** — so no imprecision propagates upward, and a week whose Daybooks were skipped or discarded still gets its Weekbook (pinned by a test that the Daybook series is never read). Register guards shared via `JournalRegisterGuards`. Mobile: the **Days / Weeks** segmented control on the Journal tab, cards labelled by their span ("Mon 3 – Sun 9 August"), and one entry page reading either book via `?cadence=` — on a Weekbook the existing fortnight chart lands as this-week-against-last-week for free, since the entry is dated by its week's last day. Renames: `DaybookPage`/`DaybookEntryPage` → `JournalPage`/`JournalEntryPage`, routes `//daybook`→`//journal` and `daybookentry`→`journalentry`, icons `icon_tab_daybook*`→`icon_tab_journal*`. *Remaining: the plan gate (separate effort — nothing is gated today), an in-app "ready" inbox item, and explained-gap cards for skipped weeks (deferred deliberately: nothing exposes **why** a week was skipped, and guessing a reason would be the invention the coverage guard exists to prevent).* |
| **CardiJournal — the Monthbook** | R2 | R2 | — | Guardian Plus | 🔶 **Shipped in dev, API and mobile.** `DigestAudience.Monthbook` + `IX_DigestEntries_OneMonthbookPerMonth`, `GenerateDueMonthbooksAsync` on the same half-hourly `--job digest` pass, `MonthbookPrompt`, `?audience=monthbook`, and the **Months** segment on the Journal tab. Due on the **1st** once the member's `MonthbookLocalTime` passes, covering the calendar month just gone and dated by its last day, written once. **Guard: at least 14 of the month's days must carry readings.** **A raw reassessment of the month from its own measurements, not a digest of its Weekbooks** — the month's days are compressed to per-week aggregates in code, so no narrative feeds another narrative. The standout is a **week**, not a day: at month scale one unusual day is noise the caregiver already read in its Daybook, while a week that ran differently is the month's shape. Composed on the 1st, so the whole month is still inside every retention window when it is read — and the **retention prerequisite is met** (`DigestRetentionMonths` 3 → 7, PR #379). The entry page draws a **30-day** window for a month, with the counted lines moved to the same window by construction so the sentence never describes more or less than the chart. *Remaining: the plan gate (separate effort — nothing is gated today).* |
| **CardiJournal — when each book is written (per-member setting)** | R1 | R1 | — | — | ✅ **Shipped** — `CardiMember.DaybookLocalTime`/`WeekbookLocalTime`/`MonthbookLocalTime`/`JournalWeekStartsOn` (all nullable, null = default), `GET`/`PUT /api/v1/cardimembers/{id}/journal-settings` (primary caregiver only for the write), and the mobile **Journal Settings** screen, reached from the Management group on member detail. Bounds live in `JournalSchedule`: 01:00–12:00 on the half hour, matching the digest job's cadence so a chosen time is always one the generator honours. All three generators now exist, so `weekbookAvailable`/`monthbookAvailable` both return **true** and every timing on the screen governs something that actually runs (they reported false while the Weekbook and Monthbook generators were outstanding). `JournalWeekStartsOn` also settles what a journal week is, per member, ahead of the Weekbook |
| Trend charts (7d/30d/90d/custom) | R2 | R2 | R2 | — | 🔶 **Mobile shipped in dev ahead of R2, at three windows not four.** Member Detail carries a **Key Metric Trends** carousel (`MetricTrendCard`, six metrics, 330dp) and each card expands to a full-screen `MetricTrendPage` — the same card given the whole screen rather than a second chart built to resemble it. `TrendWindowSelector` offers **7 / 14 / 30 days**; **90-day and custom windows are not built**. Drawn from the existing per-member dashboard response, which is also **why the windows stop at 30**: `DashboardMetric.Series` carries `MemberInsightsCalculator.SeriesDays` = **30 points**, and a client showing a shorter window takes the tail. A 90-day or custom window therefore needs more data, not just another route — either `SeriesDays` grows (and the dashboard payload grows for every caller with it) or trends get a request of their own. Web can reuse the same shape for 7/14/30 without either. Line weight halved for legibility (#561); the same chart renders into journal PDFs and graphs-only exports (#1071), and alert-detail readings are tappable with the alert's day coloured (#563). Web not started |
| Notification preferences (global + per-member, quiet hours, sensitivity) | R2 | R2 | R2 | — | 🔶 Global quiet hours + lock-screen detail + per-category mute shipped ahead of schedule alongside the push spine ([notifications.md](./execution/backend/api/notifications.md)). **Per-member scoping now shipped in dev as caregiver-defined alarms** (own row below). **Per-member on/off for the built-in rules also shipped** — `AlertPreference.DisabledRules` (sparse: absent or empty means every rule is on), `GET` and `PATCH .../cardimembers/{id}/alert-preferences[/rules/{ruleId}]`, and the mobile **Alert Settings** page presenting the rule catalogue as switches that save on flip and refuse to race each other. A caregiver can also change these from chat (#1116). What remains is **sensitivity tuning** — a rule is on or off, there is no threshold dial, and `CardiMember.AlertSensitivity` is still unused |
| **Caregiver-defined alarms (metric thresholds, account + per-member)** | R2 | R2 | — | — | 🔶 **Shipped in dev.** `MetricAlarm` / `MetricAlarmState`, `MetricAlarmsController` (8 routes incl. the builder catalogue), `MetricAlarmWorker` on a 5-minute cadence, and the mobile **Custom Alerts** list + builder reached from Member Details' Management group. CloudWatch-shaped grammar — metric · statistic · comparison · threshold · window · M-of-N · missing-data · severity — set once for the account and overridable per member, with an opt-out written as a disabled override. Firing writes an ordinary `Alert`, so quiet hours, escalation and acknowledgment are unchanged. **Coexists with the nine statistical rules rather than retuning them.** Suggested defaults and their published sources: [alarm_catalogue.md](./technical/alarm_catalogue.md); Art. 22 V4 entry recorded. *Remaining: neither mobile screen has a Figma frame (design sync), and blood pressure, weight and rhythm alarms are unbuildable until those metrics are ingested.* |
| **Subscriptions & billing (Stripe)** | R2 | R2 | R2 | — | ⬜ Not started (no Stripe integration exists yet) |
| Garmin connection | R2 | R2 | R2 | — | ⬜ Not started. **Access risk (2026-09-05):** Garmin's developer program is business-only and approval-gated, and mid-2026 reports say the request form was withdrawn with no ETA — apply now; Withings is the fallback next engine |
| **AI pipeline** (Pub/Sub → SSA → MedGemma on Cloud Run; `long_term_trend` alerts; digests) | R2 | R2 | R2 | Advanced alerts: Complete Care | 🔶 **Shipped in dev ahead of R2** — digest, aggregator, assessor and webhook receiver all live on Pub/Sub + Cloud Run; prod enablement pending the MedGemma prod deploy. LSTM dropped 2026-08-10 — SSA pre-processing + prompt-injected reference ranges instead ([llm_design.md](./llm_design.md); decision log #7) |
| Export — HL7 v2 | R2 | R2 | R2 | Complete Care | ⬜ Not started — the `ReportFormat` member exists but is **refused at validation** rather than accepted and silently ignored |
| Family summaries / digests (half-hourly MedGemma summary + history) | R2 | R2 | R2 | — | 🔶 **Shipped in dev ahead of R2** — append-only `DigestEntries` with history, read via the insights digest endpoints; prod gated with the pipeline. Now two audiences: the running `Family` summary and the once-daily `Daybook` (own row below) |
| **CardiJournal — the Daybook** (once-daily account of a finished day, from all of the day's data, clinical register) | R2 | R2 | — | — | 🔶 **Shipped in dev ahead of R2** — `DigestAudience.Daybook` entries written by the digest job at 02:00 in the member's local time, one per day, never recomputed; read via `?audience=daybook` on the insights digest endpoints (with `search`/`from`/`to`/`urgency` filters, applied before the page cap) and listed on the mobile **Journal** tab (labelled CardiJournal in the page header; it replaced the Family stub); each entry opens its own page with source-tagged fortnight charts (NSF/AHA bands named) and deterministic awareness counts — counts, never risk scores, per decision log. Register allows a precise term where it explains itself; conditions, diagnoses and treatment refused in code (`DaybookPrompt`). **Since 2026-09-14 the charts are drawn for the period the book accounts for**, and a bare term is glossed rather than the whole book discarded (#1099, #1100) — the gloss guard used to throw away the informative reply and leave the vacuous one. **Since 2026-09-18 MedGemma reads the days as JSON** rather than as vs-baseline or wellness prose (#1126, #1128), so it infers from the figures instead of paraphrasing copy, and sample phrases are no longer handed to the models for them to echo back (#1098) |
| Real-time heart-rate assessment (SSA → MedGemma severity verdict) | R2 | R2 | R2 | — | 🔶 **Shipped in dev ahead of R2** — 5-minute SSA-gated assessor over the granular store; MedGemma only on a jump; red/orange verdicts create alerts and enqueue push; prod gated with the pipeline |
| Family questionnaires (digest-proposed questions + answers) | R2 | R2 | — | — | ✅ Shipped — `MemberQuestionnaires` + questionnaires endpoints; standing vs momentary (`QuestionnaireScope`); caregiver-volunteered standing facts (`QuestionnaireOrigin.Family`, not an ask); funnel telemetry (`CardiTrack.Questionnaires`); gap-backed asking (unresolved alert / Yellow+ observation, 12-hour ceiling); dismissed and answered-permanent never re-asked; in-card delete with confirm |
| Environmental enrichment (weather/AQI context for GPS exercise sessions) | R3 | R3 | — | — | 🔶 Built but **inert** — code, schema and consent flag shipped; no Cloud Run job or scheduler provisioned, and the `googlehealth.location.readonly` scope not yet requested |
| Granular minute-grain storage (hour vectors + rollups + retention) | R1 | R1 | R1 | — | ✅ Shipped — `GranularMetricHours`/`MetricRollupsHourly`, partitioned with retention by partition drop |
| Trend interpretation (family-facing narrative, no risk scores) | R3 | R3 | R3 | Complete Care | ⬜ Not started — replaces predictive monitoring; prediction cards were descoped 2026-08-10 with the LSTM |
| Family invitations + roles (admin/staff/viewer) | R3 | R3 | R3 | Family-member limit by tier | ⬜ Not started — and the mobile entry point that implied otherwise was removed on 2026-09-18 (#1141), so nothing in the app now offers an invite it cannot honour. Not to be confused with the **device** invite above, which invites a wearer to authorize a device, not a person to join the account |
| Shared care notes + @mentions | R3 | R3 | R3 | — | ⬜ Not started |
| Multi-member comparison views | R3 | R3 | R3 | — | ⬜ Not started |
| Test-result scanning + insights | R3 | R3 | R3 | Complete Care | ⬜ Not started |
| Export — LOINC/CCD | R3 | R3 | R3 | Complete Care | ⬜ Not started |
| Activity/audit log endpoint (Admin) | R3 | R3 | R3 | — | ⬜ Not started |
| Biometric login (local gate) | R4 | R4 | — | — | ⬜ Not started |
| Offline read cache (cache-first reads) | R1 | R1 | — | — | ✅ Shipped (mobile) — plumbing in #548 (peeks, eviction, `SnapshotRefresh`, `UpdatingOverlay`, `SavedDataBanner`), then every screen in #550, #551 and #554. Cached data shows immediately and refreshes when a push arrives (#650), and the app only dates a snapshot once it has actually gone stale (#1138) |
| Offline write/sync queue | R4 | R4 | R4 (PWA) | — | ⬜ Not started |
| Push notification inline actions | R4 | R4 | R4 (browser) | — | ⬜ Not started |
| Home-screen widget / PWA install | R4 | R4 | R4 | — | ⬜ Not started |
| Apple Watch — **via Google Health only** (HealthKit bridge retired) | n/a | R3 (picker mapping) | n/a | — | 🔶 **Decided 2026-09-05: no dedicated integration.** Apple offers no server-side connection; readings arrive when the wearer shares Apple Health into the Google Health app, and the live GoogleHealth engine reads them. Remaining: map `AppleWatch` into the GoogleHealth `DeviceTypes` so the picker routes it to the Google connect flow, and a live-device check of which metrics Apple passes through ([devices.md](./execution/backend/api/devices.md) "Devices that arrive via Google Health") |
| Samsung Galaxy Watch — **via Google Health only** (Samsung Health SDK retired) | n/a | R3 (picker mapping) | n/a | — | 🔶 **Decided 2026-09-05: no dedicated integration.** Samsung's Health Data SDK is on-device only; readings arrive via Health Connect → Google Health app. Same remaining work as Apple Watch |
| Withings connection | R4 | R4 | R4 | — | ⬜ Not started (config stub, placeholder client id). Self-serve OAuth 2.0 API with webhooks; the fallback next engine if Garmin's developer program stays closed. **Oura and Whoop dropped 2026-09-05** — subscription trackers for athletes, not this product's wearers; their enum members and config blocks are cleanup |
| Export — SNOMED CT | R4 | R4 | R4 | Complete Care | ⬜ Not started |
| Enterprise / Guardian Plus (business flow) | post-R4 | post-R4 | post-R4 | Guardian Plus | ⬜ Not started (out of MVP scope) |

## Resolved Conflicts (decision log)

1. **Subscriptions**: endpoints and UI ship together in **R2** — R1 (Q4 2026) is trial-only. The trial provisions the Complete Care tier for 30 days; R2 (Q1 2027) must land billing before the first beta trials convert, so trial-expiry handling is an explicit R2 entry criterion. (Previously: API P0 "launch blocker" vs UI specs MVP 2.)
2. **FHIR R4 export**: mobile ships it in R1, web in R2 — **deliberate**: mobile is the primary caregiver surface for doctor-visit prep.
3. **Export plan-gating**: all export formats will require **Complete Care** (Basic has no export), consistent with [subscriptions.md](./execution/backend/api/subscriptions.md). **Not enforced** — a gate shipped 2026-09-06 and was removed 2026-09-07, because nothing else is gated by plan and trial-only R1 has no way to keep an account entitled once its 30 days pass. Gating lands with subscriptions in R2, alongside the tier **member limit**.
4. **`long_term_trend` alerts** require the AI pipeline and therefore ship in R2, not R1 — the five R1 alert types are statistical.
5. **AI severity taxonomy**: internal Critical/High/Medium/Low maps to user-facing red/orange/yellow/green everywhere ([llm_design.md](./llm_design.md)).
6. **Polling vs webhooks**: Worker polling **shipped as the R1 ingestion path** (originally 30-minute; default cadence reduced to **10 minutes** on Aug 9, 2026 — migration `ReduceDefaultSyncFrequencyToTenMinutes`) and remains the system of record for ingestion until R2. Webhook push subscriptions move to **R2**, delivered with the AI pipeline. The original R1 row bundled "server OAuth, webhooks" — that bundling is superseded; the matrix now splits them. **Superseded (2026-08-10): both the webhook path and the AI pipeline shipped in dev ahead of R2; prod remains gated.**
7. **AI pipeline platform**: the pipeline runs on **GCP — Pub/Sub + Cloud Run, with MedGemma served via Ollama on Cloud Run and Gemini 2.0 Flash for chat/reports** — superseding the earlier Azure Functions / Event Hubs design. This matches the deployed Terraform footprint (Cloud Run, Cloud SQL PostgreSQL, Secret Manager, `europe-west2`).
9. **Apple Watch and Samsung Galaxy Watch via Google Health only; Oura and Whoop dropped (2026-09-05).** Neither Apple nor Samsung offers a server-side data connection (HealthKit and the Samsung Health Data SDK run on the wearer's phone), and the product rule is that the wearer is never an app user — so the planned on-device HealthKit bridge and Samsung Health engine are retired. Both brands' readings reach CardiTrack when the wearer shares them into the Google Health app, which the live GoogleHealth engine already reads; the only work left is mapping the brands into the picker and a live coverage check. Oura and Whoop are subscription trackers for athletes and leave the roadmap entirely. Garmin (R2) and Withings (R4) remain the two dedicated integrations; Garmin's developer-program access is unconfirmed and must be applied for before the R2 work is scheduled. Public statement: carditrack.com/supported-devices.
8. **Push delivery pulled forward from R2 to ship alongside R1 alert generation**: with Firebase/FCM credentials provisioned (#108, PRs #173/#176/#177) and statistical alerts already producing real `Alert` rows (PRs #116/#118), the provisioning lead time that motivated an early start outweighed staying strictly wave-ordered. Quiet-hours/lock-screen preferences (originally R2) shipped with it since they share the same `NotificationPreference` surface. Per-member preference scoping, notification sensitivity tuning, and the AI pipeline's own `long_term_trend` alerts remain R2.
10. **A wearer may authorize their own device, and is still not an app user (2026-09-18).** The rule that the wearer never becomes a CardiTrack user stands — decision log #9 rests on it. What changed is who holds the provider credentials during the connect step: until now a caregiver had to sign in as the wearer to connect a device, which is both a poor ask and a poor consent record. A caregiver can now send an invite link or QR code, and the wearer completes Google's consent on an anonymous hosted page (`/connect`) that creates no account, grants no app access and sends them nothing afterwards. The invite is short-lived, pollable and revocable; the caregiver's own connect flow is unchanged and remains the default.
11. **Trend charts ship at 7 / 14 / 30 days, not 7 / 30 / 90 / custom (2026-09-18).** The windows were chosen against the card the caregiver actually reads: at 30 days the carousel card already puts a point every 11dp, and 90 days on that surface is a smear. 90-day and custom windows remain R2 scope, and the constraint is the data rather than the screen: the charts read the tail of the dashboard response's 30-point `DashboardMetric.Series`, so a longer window means extending that series for every caller or giving trends a request of their own. Web is not blocked on either for 7/14/30.

## What Remains — R1, R2 and R3

The matrix above is the detail; this is the wave-level answer to "what is left".

### R1 — Core Monitoring (Q4 2026) · Mobile MVP 1 · Web MVP 1

Mobile MVP 1 is built — all 17 Figma M1 screens. What remains is web, three external gates, and four unbuilt pieces inside otherwise-shipped rows:

| Left in R1 | Where it stands |
|---|---|
| **The web app** | Web MVP 1 does not exist — `CardiTrack.Web` is still the stock Blazor template (`Counter.razor`, `Weather.razor`). Every "web not started" row above is this one thing. The largest single piece of R1 |
| **Google restricted-scope verification + CASA** | Not started; caps the beta at 100 connected wearers |
| **Consent recording (per-metric)** | Not started. The onboarding privacy-notice card is unbuilt too, which the screens doc flags against the consent-first principle |
| **Account erasure in production** | The whole cascade is live and tested in dev; prod is a separate dispatch and has none of it. The confirmation email needs an email sender that does not exist |
| **Alert notes** (and family-notify on alert detail) | Not built — there is no `AlertNote` entity. Photos likewise |
| **Wearable battery** | Code, migration and Terraform shipped; blocked on the `settings.readonly` console change in both devices projects |
| **Live-wearer field verification** | Pending; needs a real wearer on a real device |
| **Production tenant social connections** | An ops gate on the Auth0 side; web social sign-in waits on the web app regardless |
| **iOS notification service extension** | Deferred — needs Mac-based CI this environment does not have |
| Beta with 20 families, public launch | Wave exit criteria rather than features |

Deliberately *not* R1, so not counted above: server-side "Email to…" on export (mailing PHI is its own subsystem), the member switcher (MVP 2), biometric gating of medical notes (R4), and the tier member limit (lands with billing, decision log 3).

### R2 — Management & Billing (Q1 2027) · Mobile MVP 2 (part) · Web MVP 2

Much of R2's *AI* content already runs in dev ahead of schedule. What is genuinely outstanding:

| Left in R2 | Where it stands |
|---|---|
| **Subscriptions & billing (Stripe)** | Nothing exists. The single largest R2 item, and the one the public launch depends on: R1 is trial-only, so billing must land before the first beta trials convert |
| **Plan gating + tier member limit** | Nothing is gated today. Waits on billing: export, journals and member count all need an entitlement to check |
| **Prod enablement of the AI pipeline** | Built and running in dev — webhooks, aggregator, assessor, digests, journals, real-time assessment. Prod is gated on the MedGemma prod deploy, not on more code |
| **Garmin connection** | Not started, and access is not ours to schedule: the developer programme is business-only, approval-gated, and reportedly closed. Apply first; Withings is the fallback engine |
| **Trend charts — 90-day and custom windows, and web** | 7/14/30 shipped on mobile off the dashboard's 30-point series. Longer windows need that series extended or a request of their own; web can draw 7/14/30 from today's payload |
| **Notification sensitivity tuning** | Quiet hours, lock-screen detail, per-category mute, per-member alarms and per-member on/off for each built-in rule are all shipped. What is left is the dial rather than the switch: a rule can be turned off but not made less or more eager, and `CardiMember.AlertSensitivity` is still unused |
| **Export — HL7 v2** | Not started; the `ReportFormat` member is refused at validation rather than silently ignored |
| **Export — server-side email delivery** | Not started, and the solution has **no email sender at all**. The same gap blocks the erasure confirmation email |
| **Web MVP 2 surfaces** | Web has no MVP 1 either, so R2's web scope (export, reports, account deletion) sits behind building the web app itself |
| **Journal and alarm loose ends** | Plan gates for Weekbook/Monthbook, an in-app "ready" inbox item, Figma frames for the two Custom Alerts screens, and blood pressure / weight / rhythm alarms (unbuildable until those metrics are ingested) |

### R3 — Family & Multi-Member (Q2 2027) · Mobile MVP 2 (part) · Web MVP 3

R3 is almost entirely unbuilt — it is the wave where CardiTrack stops being one caregiver's app:

| Left in R3 | Where it stands |
|---|---|
| **Family invitations + roles** (admin/staff/viewer) | Not started. The dead in-app link was removed 2026-09-18 (#1141). This is the wave's keystone — shared notes, comparison views and the tier family-member limit all assume it |
| **Shared care notes + @mentions** | Not started; alert notes and photos are unbuilt too |
| **Multi-member comparison views** | Not started |
| **Test-result scanning + insights** | Not started |
| **Trend interpretation** (family-facing narrative, no risk scores) | Not started. Replaces the descoped predictive monitoring |
| **Export — LOINC/CCD** | Not started |
| **Activity/audit log endpoint** (Admin) | Not started |
| **Apple Watch / Galaxy Watch picker mapping** | Decided, not done: map both into the GoogleHealth `DeviceTypes` so the picker routes them to the Google connect flow, then a live-device check of which metrics actually come through |
| **Environmental enrichment** | Code, schema and consent flag shipped but **inert** — no Cloud Run job or scheduler provisioned, and `googlehealth.location.readonly` not yet requested |
| **Data-completeness notifications on web** | R3 for web; shipped on API and mobile |

## Cross-References

- Product roadmap narrative: [solution_manifest.md](./solution_manifest.md) (quarters map to waves above)
- Mobile screens: [ui_screens_maui_mobile.md](./execution/ui/mobile/ui_screens_maui_mobile.md) (MVP 1–3)
- Web screens: [ui_screens_blazor_web.md](./execution/ui/web/ui_screens_blazor_web.md) (MVP 1–4)
- API priorities: [execution/backend/api/readme.md](./execution/backend/api/readme.md)
- OAuth client inventory + restricted-scope verification detail: [technical/oauth_clients.md](./technical/oauth_clients.md)
- Google verification gates (Step 6): [technical/user_onboarding_process.md](./technical/user_onboarding_process.md)
- Privacy/security posture: [compliance/dpia.md](./compliance/dpia.md), [technical/data_protection_architecture.md](./technical/data_protection_architecture.md)

---

**Document Version:** 2.7
**Last Updated:** September 18, 2026
**Owner:** Product Lead
